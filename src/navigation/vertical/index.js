import CallIcon from '@mui/icons-material/Call'

const navigation = () => {
  return [
    {
      title: 'Voice Call',
      icon: CallIcon,
      path: '/voice-calling'
    }
  ]
}

export default navigation
